
# Nuvemshop Preorder (Netlify)

Rodar local:
1) `npm install`
2) (opcional) `npm i -g netlify-cli`
3) `npm run dev`

Deploy:
- Conecte o repo no Netlify
- Build: `npm install`
- Publish: `public`
- Functions: `netlify/functions`

Env vars: `CLIENT_ID`, `CLIENT_SECRET`, `APP_URL`, `SESSION_SECRET`
